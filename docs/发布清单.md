# GitHub 发布清单

本清单用于首次发布和后续 Release。发布者应逐项核对，不能用自动测试替代素材授权与视觉验收。

## 源码与测试

- [x] Python 3.12 环境可以安装运行依赖；
- [x] 完整自动测试通过；
- [x] GitHub Actions 已配置 Windows 测试；
- [x] 公开 EXE 构建成功并通过定时退出冒烟测试；
- [x] Python 模块具有中文模块文档字符串。

## 文件与隐私

- [x] `user_assets/` 除说明文件外全部被 Git 忽略；
- [x] `.venv/`、`build/`、`dist/` 和 Python 缓存均被忽略；
- [x] 公开构建不包含 `user_assets/`；
- [x] 待提交文本中不存在本机绝对路径；
- [x] 自拍功能只读取用户最初登记的原图；
- [x] 首次提交前再次执行 `git add --dry-run .` 并人工查看文件清单。

## 角色与素材

- [x] 标准角色必须通过图形确认窗口由用户确认；
- [x] 候选文件哈希变化后旧确认自动失效；
- [x] 走路 GIF 需要第二次用户确认；
- [x] 表情符号由程序图层绘制，不依赖某一套角色图片；
- [x] 在 `ASSETS_LICENSE.md` 中将演示素材设为 CC BY-NC 4.0；
- [x] 仓库所有者已确认公开演示素材可以按上述许可证发布。

## GitHub 与 Release

- [x] 创建首次 Git 提交；
- [x] 创建远程仓库并推送 `main`；
- [x] 首次 GitHub Actions 已通过；
- [x] 创建 `v0.1.0` 版本标签；
- [x] 上传公开构建压缩包，未上传本地角色构建；
- [x] 从 Release 下载并在另一目录完成哈希、解压和启动验证。
