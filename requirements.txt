# Qt 6.11.2 is the native module reported in the Windows crash dump
# (0xc0000409 in Qt6Core.dll). Keep release builds on the tested Qt 6.8–6.10
# line until the upstream Qt/PySide6 regression is resolved.
PySide6>=6.8,<6.11
Pillow>=10,<13
certifi>=2024.8.30,<2026
keyring>=25,<27
pyobjc-framework-Cocoa>=10,<13; sys_platform == "darwin"
pyobjc-framework-Quartz>=10,<13; sys_platform == "darwin"
winrt-Windows.Media.Control>=3.2,<4; sys_platform == "win32"
uiautomation>=2.0,<3; sys_platform == "win32"
