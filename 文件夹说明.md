# 文件夹说明

## 1. 文件夹用途

本文件夹是“一图桌宠（OnePic Desktop Pet）”的干净开源候选副本，用于整理可公开的 Python + PySide6 桌宠代码、演示素材、环境脚本、测试和 Agent 执行说明。

## 2. 背景与目标

项目从私人桌宠开发版中选择性迁移，目标是让普通用户可以下载 EXE，也让 Agent 能从环境检查开始，依据一张角色图片完成素材、互动、验证和打包。私人原项目保持不动；真实照片只保存在被 Git 忽略的 `user_assets/`，不会进入公开版本。

## 3. 文件结构

| 路径 | 类型 | 作用 |
|---|---|---|
| `main.py` | Python 入口 | 解析参数并启动桌宠 |
| `src/onepic_desktop_pet/` | Python 包 | 应用生命周期、窗口、行为、配置、资源定位和确认门禁 |
| `assets/pet/` | 运行素材 | 应用直接加载的透明动作帧 |
| `assets/generated/` | 派生输入 | 重新拆分动作帧所需的最终透明图集 |
| `user_assets/` | 本地素材 | 原图、自拍、标准角色候选、动作预览和流程状态，默认不提交 Git |
| `tools/onepic_workflow.py` | Python 工具 | 登记原图并管理标准角色和走路两个人工确认门禁 |
| `src/onepic_desktop_pet/emotion_effects.py` | Python 模块 | 为任意角色绘制可缩放的表情符号增强层 |
| `src/onepic_desktop_pet/accessories.py` | Python 模块 | 绘制轻量图层，并加载 46 张透明动作与 12 套小时娃衣 |
| `src/onepic_desktop_pet/growth.py` | Python 模块 | 定义 0–8 小时成长线、专注/休息/完成动作组与正向心情 |
| `src/onepic_desktop_pet/diary.py` | Python 模块 | 仅在本机记录每日完成段、最长专注、互动与随机事件计数 |
| `src/onepic_desktop_pet/daily_report.py` | Python 模块 | 生成当天六毛工作日报并保存到本机相册 |
| `src/onepic_desktop_pet/social.py` | Python 模块 | Supabase 登录、最小状态同步、搭子/房间/串门 API 与系统凭据令牌 |
| `src/onepic_desktop_pet/social_ui.py` | Python 模块 | 搭子自习室、隐私开关和双六毛本地串门窗口 |
| `supabase/migrations/` | SQL | 六毛社交表、RLS 权限、RPC 与安全加固迁移 |
| `src/onepic_desktop_pet/activity.py` | Python 模块 | 只把前台进程名归类为音乐、办公、编程或阅读，不保存历史 |
| `src/onepic_desktop_pet/music.py` | Python 模块 | 处理指定歌曲搜索和官方网页回退，不把打开客户端当成已连接 |
| `src/onepic_desktop_pet/music_control.py` | Python 模块 | 统一 Windows 媒体 Session、macOS Apple Events、当前歌曲和媒体键基础控制 |
| `src/onepic_desktop_pet/chat_manager.py` | Python 模块 | 缓存 Agent 四态、异步路由 AI，并在检测中、掉线、超时或异常时生成完整离线陪伴回复 |
| `src/onepic_desktop_pet/chat_memory.py` | Python 模块 | 在内存中保留最近 30 轮原始聊天，并把更早内容滚动压缩为有限摘要 |
| `src/onepic_desktop_pet/wellness.py` | Python 模块 | 管理可关闭的喝水与站立休息提醒 |
| `src/onepic_desktop_pet/controls.py` | Python 模块 | 快捷口袋、工作控制气泡和连续尺寸滑块 |
| `scripts/` | PowerShell | 环境检查、安装、运行、测试和打包 |
| `agent-guide/` | 文档 | Agent 从一张图执行项目的固定流程 |
| `tests/` | Python 测试 | 验证配置、行为、素材和窗口逻辑 |
| `OnePicDesktopPet.spec` | 构建配置 | 生成 Windows 目录模式可执行程序 |
| `技术更新.md` | 维护记录 | 记录结构、技术和发布边界的变化 |

## 4. 核心流程

环境检查 → 登记原图和自拍 → 选择画风并分析身份特征 → 生成首张标准角色候选 → 用户在确认窗口确认 → 生成动作 → 走路 GIF 确认 → 自动测试 → 最终验收 → 打包 → 授权检查 → GitHub 发布。

## 5. 重要约束

- 不把私人图片复制进 `assets/`；
- 不在代码中硬编码本机绝对路径；
- 不自动安装系统软件或修改环境变量；
- 标准角色未确认时不生成动作，走路未确认时不加载或打包本地宠物；
- 自拍必须使用最初登记的原图；
- 演示角色素材公开前必须确认授权；
- 不从私人项目复制 `build/`、`dist/`、缓存、归档和错误草稿。

## 6. Agent 接手建议

先阅读本文件、`技术更新.md`、`AGENTS.md` 和 `agent-guide/AGENT_GUIDE.md`。修改 Python 文件前先读模块文档字符串；运行环境操作优先调用 `scripts/`；发布前重点检查 `user_assets/` 和 `ASSETS_LICENSE.md`。

## 7. 当前状态

公开仓库已发布多个版本。当前开发版 `v0.19.1` 将音乐核心目标恢复为在用户选择的本机播放器中随机播放陈楚生歌曲，并为网易云 Windows UI Automation 增加可用性与搜索无结果的状态区分和详细诊断日志；指定歌曲严格校验不再阻碍基础随机播放。QQ 音乐、网易云、酷狗、Apple Music 和 Spotify 继续使用独立 Adapter。服务器仍不保存任务、聊天、窗口标题、动作帧或私人素材；断网时全部原有功能继续离线运行。
