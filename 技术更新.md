# 技术更新

## 更新记录

## v0.11.0 - 2026-08-11

### 更新类型

可选搭子自习室、私人房间、轻量在线状态与双六毛本地串门。

### 修改内容

- 新增邮箱注册/登录，密码不保存，刷新令牌只进入 Windows 凭据管理器或 macOS 钥匙串；
- 使用 8 位搭子码发送申请，接受后可查看对方在线、工作/休息状态和可选的今日累计；
- 新增私人自习室名称与房间码，可查看共同房间成员的轻量状态；
- 新增“派六毛去串门”，对方接受后双方客户端各自在本地显示两只六毛共同工作，无需同步动画坐标；
- 每 30 秒仅同步昵称、外观编号、状态、计时秒数、房间和串门事件；断网不影响本地功能；
- 新增隐身、隐藏准确时长和拒绝串门开关；不提供陌生人聊天、公开定位或强制排行榜；
- Supabase 后端启用六张表的 RLS、10 条读取/写入策略、登录用户 RPC 白名单和匿名执行撤销。

### 验证方式

- 本地测试覆盖最小同步字段、令牌边界、RLS 配置、菜单入口和原有功能；
- Supabase Advisor 检查匿名函数权限、外键索引与公开表 RLS；
- 执行源码冒烟、Windows 打包、打包后冒烟和公开包隐私扫描。

## v0.10.0 - 2026-08-10

### 更新类型

修正版 36 动作、0–8 小时每日成长、可触摸陪伴、主动事件与本机工作日报。

### 修改内容

- 依据用户最终确认的动作总图重新整理 36 张 1024×1024 透明素材，统一保留六根红毛、蓝色发尾、可见小嘴、蓝色锯齿脸、黄色连体衣与粉色爱心；
- 今日专注改为 0–8 小时成长线，每小时解锁状态、动作或彩蛋，8 小时以“荒野国王”演出和纪念卡收尾；
- 计时期间轮换办公、夜读、思考和保温杯动作；休息与完成分别使用喝茶、睡觉、音乐、比心、送花和合影动作；
- 新增头部点击歪头、来回摸头开心、点肚子护住、连续五戳小生气、拖拽、长按睡觉和悬停好奇；
- 六毛会结合早中晚、连续工作、长时间无操作主动出现，并加入 UFO、海鸥、踢球、鲸鱼和低概率荒野国王等随机事件；
- 新增只在本机保存的每日陪伴统计、六毛工作日报和相册；不记录任务名称、聊天内容、窗口标题或输入内容；
- 心情只提供“悠闲的一天、慢慢进入状态、默契 +1、精神满满”等正反馈，不设置饥饿、生病或掉好感惩罚；
- 原有十四套娃衣调整为长期收藏，与当天成长线分开。

### 验证方式

- 检查 36 张动作透明背景、统一画布、六根毛、蓝色发尾、嘴巴、裁切和人物一致性；
- 测试 0–8 小时阶段、正向心情、统计跨日重置、工作日报生成和触摸行为；
- 执行完整自动化测试、源码冒烟、Windows 打包、打包后冒烟和公开包隐私扫描。

## v0.9.0 - 2026-08-10

### 更新类型

本机音乐路径、独立歌词气泡、巴布达原声交互、全幅音乐动作与十四套每小时成就。

### 修改内容

- 设置页新增 QQ 音乐、网易云音乐本地程序路径，主动播放时优先使用用户选择的客户端；
- 歌词/歌名意象改用独立计时器，默认约八分钟一次，并支持 2–120 分钟连续设置；
- 支持读取用户有权使用的本地逐行 TXT，公开仓库和安装包不内置受版权保护的完整歌词；
- 双击右键六毛会播放“巴布达”，可轮换同目录多段音频并略微变化语速；无本地音频时回退到系统语音；
- 吉他、打鼓和耳机改为用户确认的全幅透明动作素材，保持六根毛与统一画布；
- 新增梦想清扫员、飞行梦想家、甜梦抱抱和红毯小大人四套娃衣，累计十四小时解锁最终“荒野国王”；
- 私人原视频、提取音频、本机路径和歌词文件继续只保存在本地，不上传 GitHub。

### 验证方式

- 测试本地路径优先级、歌词编码与限制、音频变体发现、十四套解锁和公开构建隐私边界；
- 渲染三套音乐动作及十四套娃衣联系表，检查透明背景、六根毛、裁切和尺寸一致性；
- 执行完整测试、源码冒烟、Windows 打包、打包后冒烟和隐私扫描。

## v0.8.0 - 2026-08-10

### 更新类型

快捷口袋收起逻辑、音乐客户端直连、AI 连接检测、每小时成就与音乐动作视觉修正。

### 修改内容

- 快捷口袋只由双击六毛切换显示；点击任一功能后立即收起，闲置八秒或移出三秒后自动隐藏；
- AI 设置新增“检测是否连接”：Codex 使用官方 `codex login status` 退出码，Claude Code 读取认证状态，API 通道只读请求 `/models`；
- Windows 优先直接启动已安装的 QQ 音乐或网易云音乐，短暂聚焦客户端、自动搜索随机歌名并尝试播放第一条；
- macOS 将正版搜索地址交给指定音乐客户端；未安装客户端或启动失败时才回退到官方网页；
- 手动音乐动作两分钟内不再被应用感知覆盖，随机音乐动作保持三分钟；
- 放大耳机、吉他和鼓，增强颜色对比并调整位置，避免动作存在但看不清；
- 成就门槛由八小时改为每累计一小时一套，共十套；首套改为折纸帽，最后一套固定为金皇冠与披风“荒野国王”；
- 版本更新为 `v0.8.0`，公开包继续排除私人素材、聊天记录、令牌和本机计时文件。

### 验证方式

- 测试覆盖本机 Agent 登录检测、API 只读检测端点、客户端缺失网页回退、快捷口袋切换与每小时成就；
- 批量渲染音乐动作及十套配饰，重点检查耳机、吉他、鼓和荒野国王的可见度；
- 执行完整测试、源码冒烟、Windows 打包、打包后运行和 GitHub 三平台发布构建。

## v0.7.0 - 2026-08-10

### 更新类型

六毛情境陪伴、工作控制、Claude Code 联动、音乐动作、健康提醒与八小时成就。

### 修改内容

- 文件与应用继续叫 `Lili`，角色称呼恢复为“六毛”，人格设定为带孩子气的“小小大人”；
- 新增 Claude Code 本地登录通道，采用一次性、无工具、无会话持久化调用；Codex 仍使用临时只读会话；
- 计时期间在六毛身前绘制电脑，点击电脑或摸头出现独立暂停/结束气泡，删除菜单里的重复控制；
- 双击六毛打开快捷口袋，减少对 Windows 托盘与 macOS 菜单栏的依赖；
- 默认高度从 180 缩小到 160，新增 100–360 像素连续滑块，所有动作共享统一画布尺寸；
- 前台应用只被归类为音乐、办公、编程、阅读或其他，据此切换耳机、电脑、写字和看书动作，不记录标题；
- 新增吉他、鼓、耳机动作，以及网易云音乐/QQ 音乐的正版歌曲搜索入口；
- 新增可关闭的“巴布达”语音、喝水提醒、站立休息、整点报时和歌名意象原创短句；
- 每累计工作八小时解锁一套娃衣或配饰，共十套，并保留终身累计秒数；
- 对话框改为约六成可见的浅灰蓝半透明背景、细灰边框，移除红色 PPT 式视觉，macOS 优先苹方简体；
- Windows 使用不抢焦点的原生置顶补强，macOS 使用 Qt Tool/WindowStaysOnTop 与周期恢复；
- 未内置歌词、音频或私人素材；只保存歌名及原创意象句，实际播放由用户在正版平台确认。

### 验证方式

- 完整测试覆盖 Claude stdin 安全调用、应用分类、正版音乐域名、双提醒、八小时解锁及原有动画；
- 本地批量渲染电脑、耳机、吉他、鼓和十套配饰，检查六根毛、蓝色脸、透明背景与统一画布；
- Windows 源码冒烟测试与 PyInstaller 构建完成后扫描公开包，确认不包含 `user_assets`、令牌或聊天记录。

## v0.6.0 - 2026-08-10

### 更新类型

Lili 品牌升级、可选 AI 联动、聊天界面、饮品、多段动作、自动牢骚与报时。

### 修改内容

- 应用显示名和公开构建产物改为 `Lili`，版本更新为 `v0.6.0`；
- 默认保持纯离线，新增 Codex、DeepSeek、Kimi 和其他 OpenAI 兼容 API 通道；
- Codex 通过本机 `codex exec` 的临时、只读会话联动，找不到、未登录或连接失败时自动回退到离线回答；
- DeepSeek 默认使用 `deepseek-v4-flash`，Kimi 默认使用 `kimi-k3`，API 地址和模型允许用户修改；
- API 令牌通过系统安全凭据库保存，非敏感连接设置保存在 Lili 本地设置目录；
- 单行输入框替换为圆角暖色聊天面板，显示提供方、隐私提示、对话区域和 AI 设置入口；
- 新增咖啡、热茶，以及伸展、思考、安静陪伴、击掌等多段组合动作；
- 新增随机本地牢骚和整点报时，两项均可开关，报时默认关闭；
- 旧“六毛工作搭子”的窗口尺寸与工作计时可在首次启动时兼容读取或迁移；
- 公共发布包继续排除 `user_assets`、私人照片、API 令牌、聊天记录和本机设置。

### 验证方式

- AI 请求、Codex JSONL 解析、HTTPS 地址校验和无明文令牌配置均使用模拟测试；
- 窗口测试覆盖新聊天面板、饮品、多段动作、牢骚与报时开关；
- 发布前运行完整测试、源码冒烟测试、Windows 构建和公开包隐私扫描。

## v0.7 - 2026-08-10

### 更新类型

工作计时、长期工作劝慰、陪伴动作与鼓励性对话增强。

### 修改内容

- 新增只保存在本机的今日工作计时，支持开始、暂停、完成和查看累计时长；
- 计时跨应用重启保留当天累计，日期变化自动归零，自然退出时自动暂停；
- 连续工作约 25 分钟显示投入鼓励，约 50 分钟提醒喝水活动，90 分钟后更明确地劝休息；
- 新增陪我专注、为我加油、给我抱抱、庆祝完成、安慰我和提醒休息六类动作；
- 扩充工作压力、自我怀疑、拖延、犯错、孤独、委屈和爱意等离线话题回应；
- 右键菜单和系统托盘都增加工作计时与陪伴动作入口；
- 公开版本号更新为 `v0.5.0`，继续沿用“六毛工作搭子 / SixHairWorkmate”名称。

### 隐私边界

- 工作记录仅包含本地日期与累计秒数；
- 不保存任务名称、对话文本或提醒历史；
- 不访问网络，不把本地工作数据打进公开安装包。

### 验证方式

- 纯逻辑测试覆盖计时开始、暂停、重复开始、跨重启累计、日期归零、格式化和三档提醒；
- 窗口测试覆盖陪伴动作、工作开始、50 分钟劝休息、完成庆祝和菜单入口；
- 发布前继续执行完整测试、三平台构建与公开包隐私检查。

## v0.6 - 2026-08-10

### 更新类型

六毛工作搭子命名、默认尺寸优化、喂食与离线对话。

### 修改内容

- 应用显示名称和公开安装包名称改为“六毛工作搭子 / SixHairWorkmate”；
- 默认显示高度从 220 调整为 180，并使用独立设置目录避免旧尺寸覆盖；
- 新增苹果、小饼干和热牛奶三种喂食选项及饱食度反馈；
- 新增桌面对话输入框、跟随人物的文字气泡和常见工作/情绪话题回复；
- 对话完全离线处理，不保存聊天历史，不访问网络；
- 托盘与右键菜单新增喂食、对话和查看状态入口；
- Windows 与 macOS 构建产物统一改用 `SixHairWorkmate` 名称。

### 验证方式

- 喂食增益、吃饱拒食、未知食物、工作对话、疲惫对话和回退回复均有纯逻辑测试；
- 窗口测试覆盖默认 180 高度、喂食气泡与本地对话气泡；
- 公开打包仍禁止包含 `user_assets`、私人照片和聊天记录。

## v0.5 - 2026-07-17

### 更新类型

角色确认、素材授权和首次发布准备。

### 修改内容

- 将保留原画风的白裙长辫标准角色登记为候选，并由用户在图形窗口中明确确认；
- 标准角色候选哈希已锁定，旧错误候选和旧走路审批不会被复用；
- 将 `assets/` 中的公开演示美术素材设为 CC BY-NC 4.0；
- 明确程序代码和文档继续采用 MIT License，`user_assets/` 不进入公开仓库；
- 补充 GitHub 发布清单并完成本机路径、常见密钥、大文件和忽略规则审查。

### 修改原因

- 完成标准角色的人机确认闭环；
- 让代码与美术素材使用不同且明确的授权，避免开源边界含糊；
- 确保首次提交不携带本地角色、原图、构建缓存或机器相关配置。

### 影响范围

- 影响素材授权说明、README、发布清单和本地角色工作流状态；
- 标准角色仍位于被 Git 忽略的 `user_assets/`，不会进入公开提交。

### 验证方式

- 用户通过图形确认窗口批准标准角色；
- 工作流显示标准角色为 `approved`，走路仍为 `not_generated`；
- `git add --dry-run .` 未包含 `user_assets`、`.venv`、`build` 或 `dist`；
- 公开文本未发现本机绝对路径或常见密钥格式。
- 首次提交已推送到公开仓库，远程 README 可读取；
- 首次 GitHub Actions `tests` 工作流结论为 `success`。

### 遗留问题

- 新标准角色的惊讶动作与八相位走路仍需在后续生成和视觉验收。

### 后续待办

- [x] 创建首次 Git 提交并推送 GitHub；
- [x] GitHub Actions 首次运行通过；
- [x] 发布 `v0.1.0` 公开构建并执行下载后验证。

### 发布后验证

- `v0.1.0` 标签已推送；
- Windows ZIP 大小为 58,149,391 字节，包含 216 个条目且不含 `user_assets`；
- GitHub 下载文件 SHA-256 为 `E8D83DCFDCA8FDD5D8F7B0DEDD4A7590A3DA349AE4D7D9268DBF7BBEE8F1CD0C`；
- 下载包解压后 `OnePicDesktopPet.exe --smoke-test-ms 1500` 退出码为 0。

## v0.4 - 2026-07-17

### 更新类型

开源流程修正、表情交互增强和审批安全加固。

### 修改内容

- 新增与人物图片解耦的矢量表情符号层，覆盖开心、害羞、惊讶、生气、困倦、疑惑、自拍、挥手和拖拽；
- 表情符号支持高 DPI、尺寸调整、轻微漂浮动画和透明窗口轮廓；
- 将固定 Q 版流程改为保留原画风、轻度 Q 版、完整 Q 版三种选择；
- 角色候选登记后保存 SHA-256，候选内容发生变化时旧确认自动失效；
- 移除 `approve-character --yes`，改为显示候选图的用户确认窗口；
- 新增错误角色撤销能力，撤销后同步废止旧走路审批；
- 增加测试专用演示素材模式，使公开测试不受本机 `user_assets/` 污染；
- 明确惊讶动作为微张嘴、单手轻放胸前的克制反应，禁止双手高举和大张嘴尖叫模板。

### 修改原因

- 让表情增强适用于开源后新增的任意角色，不需要把符号画死在素材中；
- 防止 Agent 用一个命令代替用户确认，也防止修改候选后复用旧审批；
- 尊重输入角色原有画风和辨识度，不强制把所有角色变成相同 Q 版模板。

### 影响范围

- 影响桌宠窗口绘制、标准角色登记与确认、Agent 执行说明、素材规范和测试；
- 不改变自拍必须使用登记原图、走路需单独确认和公开构建默认排除 `user_assets/` 的规则。

### 验证方式

- 完整 38 项测试通过；
- 表情状态映射和效果计时器均有自动测试；
- 已生成八种状态联系表并完成视觉验收，符号不依赖系统字体；
- 公开 EXE 重新构建成功，冒烟测试退出码为 0，构建目录不包含 `user_assets/`；
- 修改后的候选哈希无法通过审批；撤销角色后动作与走路门禁恢复为未完成。

### 遗留问题

- 新标准角色预览已经生成，但尚待用户明确确认并登记；
- 惊讶动作和全套动作必须在标准角色确认后重新生成并视觉验收。

### 后续待办

- [x] 生成一张保留原画风和身份特征的标准角色预览；
- [ ] 用户在确认窗口验收候选；
- [ ] 以确认图为唯一参考重做自然惊讶动作和八相位走路。

## v0.3 - 2026-07-17

### 更新类型

流程修正、动作优化和隐私功能完善。

### 修改内容

- 新增 `onepic_workflow.py` 和私有 `workflow.json` 状态；
- 登记原图时保留原始文件副本，并自动生成同分辨率 `selfie.png`；
- 新增首张 Q 版候选登记和显式确认门禁，未确认时禁止批量生成动作；
- 新增八帧走路 GIF 预览和显式确认门禁，未确认时不加载或打包私有宠物；
- 走路移动曲线由“落脚完全冻结后猛跳”改为 16 ms 亚像素减速—加速曲线；
- 默认水平速度从约 125 像素/秒调整为约 62.5 像素/秒；
- 公开构建默认排除全部 `user_assets/`，个人构建必须显式传入 `-IncludeUserAssets`；
- 更新 README、Agent 规则、执行说明书、素材规范和验收清单。

### 修改原因

- 防止不同人物被生成成相同模板脸；
- 防止未经确认的角色继续生成大量错误动作；
- 减少走路平移、停顿和突然加速；
- 确保自拍使用最初上传的原照片，而不是生成动画帧。

### 影响范围

- 影响私有角色生成、加载、个人构建和 Agent 执行顺序；
- 公开演示角色仍可直接运行，公开构建不含用户私有素材。

### 验证方式

- `35 passed`；
- 原图与自拍图像尺寸均为 `381×524`；
- Q 版未确认时 `prepare_user_pet.py` 返回退出码 2；
- Q 版或走路未确认时个人构建被阻止；
- 公开构建不存在 `_internal/user_assets`；
- 公开 EXE 冒烟测试退出码为 0。

### 遗留问题

- 当前测试原图尚未生成并确认新的 Q 版候选；
- 旧的偏写实全身补全图不会被自动视为已确认 Q 版。

### 后续待办

- [ ] 针对当前原图生成 2～3 个保留个人特征的 Q 版候选；
- [ ] 用户确认其中一个候选后重新生成动作；
- [ ] 展示新走路 GIF 并完成第二次确认。

## v0.2 - 2026-07-17

### 更新类型

环境验证、测试验证和构建验证。

### 修改内容

- 将 PowerShell 脚本保存为 Windows PowerShell 5.1 可识别的 UTF-8 BOM；
- 在项目内创建 `.venv` 并安装 Python 3.12 开发依赖；
- 从最终透明图集重新生成 38 个动作帧；
- 调整无私有自拍照片时的横竖图高 DPI 测试；
- 生成 `dist/OnePicDesktopPet/OnePicDesktopPet.exe`；
- 检查构建目录的私人图片和用户素材内容。

### 修改原因

- 确认环境脚本、源码、素材管线和 Windows 构建均可复现；
- 保证公开候选包不会携带私人自拍照片。

### 影响范围

- 只影响 OnePic Desktop Pet 开源候选副本；
- `.venv/`、`build/` 和 `dist/` 均被 Git 忽略。

### 验证方式

- 环境检查通过：Python 3.12.9、Git 2.51.2；
- `32 passed`；
- 源码冒烟测试通过；
- 打包 EXE 以退出码 0 完成 1.5 秒启动测试；
- 构建目录未发现 `selfie-photo-private`、`assets/personal` 或 JPG/JPEG 私人图片。

### 遗留问题

- 公开提交前仍需仓库所有者确认演示角色素材的授权范围。

### 后续待办

- [ ] 确认素材授权条款；
- [ ] 创建首次 Git 提交；
- [ ] 创建 GitHub 远程仓库和 v0.1.0 Release。

## v0.1 - 2026-07-17

### 更新类型

初始化、结构调整和隐私隔离。

### 修改内容

- 新建独立的 OnePic Desktop Pet 干净开源候选副本；
- 将 Python 包整理到 `src/onepic_desktop_pet/`；
- 迁移最终演示动作帧和必要透明图集，不迁移原始照片、错误草稿、缓存与构建产物；
- 将应用、配置目录和打包名称统一为 OnePic Desktop Pet；
- 自拍照片改从被 Git 忽略的 `user_assets/` 读取；
- 新增环境检查、安装、运行、测试和打包脚本；
- 新增 README、隐私说明、素材规范和 Agent 执行说明书。

### 修改原因

- 建立可公开、可复现且不会误传私人照片的 GitHub 仓库基础；
- 让 Agent 从环境检查开始执行，而不是只提供一段提示词。

### 影响范围

- 只影响新建的开源候选副本；
- 不修改原有的私人桌宠开发版。

### 验证方式

- 本记录创建时尚未验证；验证结果见 v0.2。

### 遗留问题

- 正式公开前需要确定演示角色图片的最终授权条款；
- 尚未创建 GitHub 远程仓库和 Release。

### 后续待办

- [ ] 完成干净环境测试；
- [ ] 完成 Windows 构建并检查打包资源；
- [ ] 确认素材授权后创建 GitHub 仓库。
