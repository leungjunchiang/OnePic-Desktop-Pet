# 技术更新

## 更新记录

## v0.22.80 - 2026-08-17

- 快捷图标悬浮标签改为浅色背景、黑色文字，提高在白色桌面和浅色窗口上的可读性。

## v0.22.79 - 2026-08-17

- 移除普通左击六毛时的工作控制条触发逻辑，避免“开始/暂停/结束工作”按钮遮挡桌宠。
- 左击恢复为纯宠物互动；工作计时仍由快捷栏、任务栏/Dock 和其他明确工作入口控制。

## v0.22.78 - 2026-08-17

- 快捷口袋增加统一的六毛风格 hover 标签，聊天、工作、自习室、音乐、设置都能在悬停时显示名称。
- 音乐入口专门显示“音乐”，避免用户仅凭图标猜测功能。
- 保留 `QPushButton.toolTip` 与 `accessibleName`，兼容系统 tooltip 和辅助功能读取。

## v0.22.77 - 2026-08-17

- 快捷口袋改为统一轻量托盘，按钮边框、阴影和交互高亮更克制，图标留白与间距统一。
- 六毛本体右键新增“六毛互动”子菜单，包含陪伴动作、状态查看和“喂食与饮品”。
- 喂食入口继续调用既有 `feed_pet()` 与 `CompanionModel`，不复制状态逻辑。

## v0.22.76 - 2026-08-17

- 右键六毛本体改为专属公仔菜单：换动作、换娃衣、换装与外观、隐藏六毛。
- 动作菜单直接复用 `ACTION_GROUPS` 与现有动作执行逻辑；娃衣菜单复用解锁/装备逻辑，避免第三套状态。
- 聊聊、工作、自习室、音乐、待办、AI 设置、更新等入口继续由双击快捷栏、任务栏/Dock 和设置承担。

## v0.22.75 - 2026-08-17

- 右键六毛本体改为直接显示当前工作控制条，不再弹出完整菜单；完整菜单继续由任务栏/Dock 承担。
- 工作控制按 `IDLE → 开始工作`、`FOCUSING → 暂停工作/结束工作`、`PAUSED → 继续工作/结束工作` 动态收口。
- 控制条默认定位在六毛头顶上方，采用固定窗口几何跟随六毛，避免动画帧抖动并支持屏幕边缘避让。
- 工作操作完成、再次右键或点击其它 Qt 窗口后自动收起。

## v0.22.74 - 2026-08-17

- 音乐快捷功能不再显示 Now Playing 信息或持续读取媒体 Session，只保留实际控制操作。
- 暂停/继续与结束工作按钮归属六毛本体，显示在六毛下方并跟随整体窗口移动。
- 快捷坞默认在六毛头顶上方留白显示，并在边缘空间不足时自动切换位置。

## v0.22.73 - 2026-08-17

- 快捷栏改为跟随六毛整体移动的红黄蓝图标快捷坞，定位基于窗口位置而不是动画帧轮廓。
- 设置、更新和关于入口进一步收口，一级菜单只保留高频操作与系统显示/退出。
- 保留图片清晰度和既有内容资源，不改变资源文件。

## v0.22.72 - 2026-08-17

- Supabase 认证刷新统一进入 `AuthSessionManager`，Direct/Proxy 共用 session、generation 和 single-flight refresh lock；网络检测只执行轻量健康探针。
- 对 refresh token 轮换失败增加最新持久化 session 恢复和用户友好提示；日志只记录请求来源、刷新阶段和 generation，不记录完整令牌。
- 右键菜单按高频、设置、系统动作分区；快捷口袋改为五个红黄蓝矢量图标，Windows/macOS 不再依赖 Emoji 字形。
- Windows GSMTC 与 macOS Apple Events 统一广播 `MusicState`，界面不再把“播放中但暂时读不到 metadata”误显示为“暂无”。
- PyInstaller 排除确认未使用的开发/数据科学依赖；公开宠物图片保持原始资源和清晰度，后续大内容继续使用 GitHub 增量包。

## v0.22.70 - 2026-08-17

- 历史记录消息列表根据窗口宽度自动计算换行高度，长消息可以完整阅读，不再出现列表行高度不足导致的截断。
- 保留独立任务栏/Dock 窗口、会话名称编辑、单条消息编辑/删除和新对话管理入口。

## v0.22.69 - 2026-08-17

### 聊天记录窗口与按钮交互完善

- 移除聊天窗口中“⋯ + 下拉箭头”的隐藏管理入口，改为直接显示“聊天记录”和“新对话”。
- 历史记录使用独立顶层窗口，具备独立任务栏/Dock 项、最小化、最大化和关闭行为。
- 支持编辑会话名称、编辑或删除单条消息、删除单段会话；删除当前会话时同步清理 AI 上下文，不影响待办和提醒。
- 历史窗口提供清空当前显示、新对话、删除全部记录等明确操作；AI 回复生成期间只读，避免编辑和流式写入冲突。

## v0.22.68 - 2026-08-17

### 聊天记录管理与独立新对话

- 聊天窗口新增“聊天管理”菜单，分别提供清空当前显示、开始新对话和查看聊天记录。
- “开始新对话”会清除 `ConversationMemory` 与本地 Codex App Server thread ID，但不删除 Todo、Reminder 或时光轴数据。
- 新增本地 `chat-history.json` 会话记录，最多保留 20 段会话，每段最多 120 条消息；历史记录窗口支持查看和全部删除。
- 删除全部聊天记录会同步重置 AI 上下文，并通过二次确认明确提示待办不会受到影响。

## v0.22.67 - 2026-08-17

- 修复“你能不能给我设置一个明天3点写论文的待办”这类礼貌请求的标题清洗，保存结果为“写论文”，并兼容修正此前带有整句前缀的旧任务。
- 紧凑待办栏改为展示今天及未来 7 天内的普通待办；未来任务显示“明天/还有几天”日期提示，今日统计与日报仍保持只看今天。
- 增加明天任务写入、未来视图和旧错误标题去重修复的回归测试。

## v0.22.66 - 2026-08-17

- Lili 聊天改用长期存活的 Codex App Server：初始化一次、创建或恢复六毛 thread，后续消息通过 `turn/start` 复用上下文。
- 接收 `item/agentMessage/delta` 增量事件，聊天窗口可先显示首字；停止按钮通过 `turn/interrupt` 中断当前回答，重新打开时恢复本地 thread。
- Windows 与 macOS 统一使用 `gpt-5.6-luna`：普通聊天无推理、复杂聊天低推理，极复杂问题才切换 Terra；连接异常自动回退到原有 Codex CLI 调用。
- 明确的日期/时间待办继续优先由本地解析并真实写入 Todo/Reminder，避免仅识别为普通聊天。

## v0.22.65 - 2026-08-17

- Lili 在 Windows 与 macOS 上默认通过一次性 Codex CLI 配置使用 `gpt-5.6-luna`，并保持低推理模式，以缩短日常对话等待时间。
- 保留 `LILI_CODEX_MODEL` 的显式模型覆盖与 `off`/`none`/`default` 关闭选项；模型不可用时继续回退到 Codex 默认模型。
- 增加 Windows 平台的命令构造回归测试，不改变普通 Codex CLI 或 Codex App 的全局配置。

## v0.21.8 - 2026-08-13

- Supabase 自习室在线状态心跳修复：恢复已登录角色执行 RLS 使用的安全辅助函数权限，避免 heartbeat/dashboard 返回 403。
- 保留客户端即时心跳、房间状态实时渲染与 `focus/rest/offline` 状态显示，修复两台电脑都在工作却互相显示为休息。

## v0.22.0 - 2026-08-13

- 自习室共享状态继续按 `room_id` 隔离，新增共同任务倒计时、房间仪式时间、共同挑战、房间事件流和离开总结；离开房间后清空成员展示。
- 互动增加快速短语、对方六毛气泡反馈、15 秒客户端/服务端双重冷却和同房成员校验；新增可选的搭子开工/下班状态订阅。
- 本机专注增加第几轮、连续天数、最长连续、本周累计、与昨天差值、应用类别切换/离座质量评分、7 日高效时段、晚间节奏和明日第一件事。
- 键鼠无输入自动暂停并在返回时询问离座时间是否算休息；会议、演示、游戏和全屏场景自动抑制气泡、串门和提醒。
- Supabase 新增 `lili_room_schedules`、`lili_room_challenges`、`lili_buddy_subscriptions` 及 RLS、索引和 RPC；本机专注细节仍只保存在本机。

## v0.21.5 - 2026-08-13

- 自习室和串门改为普通可最小化的独立窗口，后台同步不再抢回焦点。
- SocialClient 增加可配置的轻量 Social API 后端，默认仍兼容 Supabase。

## v0.21.4 - 2026-08-13

### 更新类型

Supabase 自习室连接验证、窗口最小化与手动换装持久化。

### 修改内容

- 自习室使用普通独立窗口，启用系统菜单、最小化和关闭按钮，不置顶；
- 自习室最小化后再次点击菜单会恢复原窗口，不重复创建，也不影响桌宠计时与动画；
- 累计工作时长只解锁小时娃衣，用户手动选择的外观不会被成长线覆盖；
- 已连接 Supabase 项目并验证数据库健康、迁移、RLS 表与自习室数据；
- 保留 v0.21.3 的音乐首次点击重试、后台执行和前台恢复能力。

### 验证方式

- Supabase 项目 `zkgctfntrioffpifiggk` 状态为 `ACTIVE_HEALTHY`；
- 6 张自习室相关表均存在并启用 RLS，数据库只读验证查询成功；
- 全量测试与 Python `compileall` 通过。

## v0.21.3 - 2026-08-13

### 更新类型

音乐首次点击重试、后台执行与前台焦点恢复。

### 修改内容

- 随机选歌的搜索结果加载和播放动作各自最多自动重试一次，用户不需要重复点击六毛；
- 网易云 Default Desktop 本机回退失败时自动重跑一次完整搜索/点击序列；
- 音乐命令继续由 `MusicCommandThread` 后台执行，PowerShell 使用 `CREATE_NO_WINDOW`，不再闪出控制台窗口；
- 网易云只在发送键鼠输入所必需的短时间内激活，完成后恢复用户原来的前台应用，不再保持播放器置顶；
- 明确记录每次搜索、播放尝试和恢复前台结果，便于继续排查真实播放器差异。

### 验证方式

- 全量测试通过；
- 新增搜索未加载、首次播放动作失败、网易云 native 回退重试和前台恢复脚本回归测试；
- Python `compileall` 通过。

## v0.21.2 - 2026-08-13

### 更新类型

Windows UIAutomation 搜索异常分类与网易云本机回退修复。

### 修改内容

- PowerShell UIAutomation 超时、RootElement 为空、窗口/搜索框不可访问等情况统一归类为 `UI_AUTOMATION_UNAVAILABLE`，不再误报 `SEARCH_FAILED`；
- 网易云在 UIAutomation 包装成普通搜索异常时也会继续尝试独立的 Default Desktop 随机播放回退；
- QQ 等没有独立 native 回退的播放器会明确显示 UIAutomation 不可用，便于用户切换到可用播放器；
- 保留点击后轮询 GSMTC `Playing` 状态和陈楚生元数据确认逻辑。

### 验证方式

- 新增 UIAutomation 异常分类与网易云 native fallback 回归测试；
- 全量测试与 Python `compileall` 通过。

## v0.21.1 - 2026-08-13

### 更新类型

网易云/QQ 自动选歌成功判定与 GSMTC 播放状态确认修复。

### 修改内容

- 网易云 Default Desktop 回退点击歌曲后不再要求窗口标题出现“陈楚生”；标题仅记录为辅助诊断信息，真正的播放状态交给 Python 侧目标 GSMTC Session；
- 随机歌手播放在发出 Adapter 播放动作后最多等待 5 秒，轮询目标 Provider 的 `playback_status`，进入 `Playing` 即记录 `PLAYBACK_STARTED_UNVERIFIED`；
- 读取到当前歌手包含陈楚生时升级为 `PLAYBACK_CONFIRMED`，媒体 Session 暂不可读不会阻止已经发出的播放动作；
- 所有 Provider 尝试失败时在用户提示中附带每个平台的内部阶段码，并继续写入 `requestedArtist`、选中结果、当前歌曲和失败阶段 debug 日志。

### 验证方式

- 全量测试通过；
- Python `compileall` 通过；
- 保留 v0.20.1 网易云真实桌面联调路径，当前环境不伪造新的交互式播放器验收结果。

## v0.21.0 - 2026-08-13

### 更新类型

Song Resolver、Transport Controller 与播放器真实状态机重构。

### 修改内容

- 新增 `MusicSongResolver`，只负责搜索歌手、选择歌曲、调用独立 Provider Adapter，并在单个平台失败后继续自动回退；
- 新增 `MusicTransportController`，只负责 Play、Pause、Toggle、Next、Previous 和元数据读取，基础控制路径不会调用搜索或 UI Automation；
- Windows Transport 固定使用“目标 Provider GSMTC Session → 短暂等待并重新获取目标 Session → 确认目标应用运行后才使用系统媒体键”的顺序，不使用 `GetCurrentSession()` 误控浏览器或其他播放器；
- 明确增加 `APP_STARTED`、`WAITING_SESSION` 与 `PLAYING` 状态，区分已安装、已启动、等待 Session、控制可用与正在播放；
- 用户明确设置优先播放器时，首次基础播放允许“启动目标应用 → 等待 Session → 控制”；默认自动模式没有真实活动 Session 时不会随意启动任意客户端；
- 自动选歌成功后立即把 Provider 交接给 Transport，之后的播放/暂停/上一首/下一首锁定该播放器；自动选歌失败不会把基础控制误报为连接失败；
- 设置页分别显示应用状态、基础播放控制、自动选歌实验能力和当前歌曲，安装发现不再使用“已连接”措辞。

### 验证方式

- 验证基础控制不会调用 Song Resolver Adapter；
- 验证明确选择 QQ 音乐时会启动目标应用、等待对应 Session，并直接向该 Session 发送 Play，不触发全局媒体键；
- 验证运行中但无 Session 时先等待，再使用媒体键 fallback；
- 验证自动选歌失败仍明确提示基础 Transport 可以继续使用；
- 音乐、播放器 UI 与窗口专项回归测试通过。

## v0.20.1 - 2026-08-12

### 更新类型

网易云 3.x 真实桌面调度、高分屏坐标修复与随机陈楚生歌曲实机验收。

### 修改内容

- 查明 Codex 后台桌面与用户 `Default` Desktop 隔离，普通 UI Automation 只能得到空根节点；新增绑定 `Default` Desktop 的 STA 本机控制线程；
- 网易云 3.x 仅暴露 `OrpheusBrowserHost` Chromium 外壳时，切换为 Per-Monitor DPI aware 物理像素交互，修复 150% 缩放下搜索框和播放按钮点偏；
- 回退流程固定为搜索 `chenchusheng`、进入明确属于陈楚生的专辑/歌曲列表、从可见且标有陈楚生的歌曲行随机双击一首；
- 删除该回退中的全局 Play 和“下一首”动作，防止续播原队列或逸出到其他歌手；
- PowerShell 控制输出固定按 UTF-8 解码，中文曲目标题不再因系统 GBK 默认编码被误判为失败；
- UI Automation 不可用时，`BasicRandomArtistPlaybackManager` 会调用 Provider 独立的本机随机播放能力，而不是直接结束自动回退。

### 验证方式

- 在本机网易云真实客户端、150% Windows 缩放和用户 `Default` Desktop 中连续执行 5 次完整调度，5/5 成功；
- 实际标题包括“获奖之作 - 陈楚生”“晓得 (Live版) - 陈楚生/姚晓棠”“风起时，想你 - 陈楚生”，未再次播放其他歌手；
- 从 `MusicProviderManager.play_random_artist_auto()` 真实入口验证自动选择网易云并成功，不依赖 mock；
- 完整 Python 测试 163 项通过，源码编译与差异检查通过。

## v0.20.0 - 2026-08-12

### 更新类型

音乐 Provider 自动选择、跨平台失败回退、成功历史学习与菜单展开符号修复。

### 修改内容

- `MusicProviderManager` 在首次随机播放时统一检测 QQ音乐、网易云、酷狗、Apple Music 与 Spotify 的安装、运行、真实窗口、媒体 Session、基础控制和随机歌手播放能力；
- 根据运行状态、控制能力、窗口、Session、安装状态、最近成功和连续失败计算可解释评分，并记录 `provider`、`score`、`reason`、`result` Debug 日志；
- “随机听一首陈楚生”不再绑定手动选择的平台：首选失败后自动尝试下一个已安装 Provider，只有全部失败才提示用户重试；
- 播放动作成功且媒体歌手包含陈楚生时标记 `PLAYBACK_CONFIRMED`；暂时无法读取媒体信息时标记 `PLAYBACK_STARTED_UNVERIFIED`，继续播放而不主动停止；
- Provider 成功/失败次数、连续失败、最近时间与错误码保存在非敏感本机设置中；成功平台获得后续加权，UI Automation 不可用和连续失败会降权；
- 播放/暂停、下一首和上一首锁定最近真正开始播放的 Provider，不会在基础控制时重新切换平台；
- 设置页默认显示“自动选择（推荐）”、当前使用和已检测平台；手动路径与优先播放器仅作为高级选项；
- 删除一级子菜单标题中手写的 `›`，只保留 Qt/系统绘制的原生展开箭头，修复双扩展符号。

### 验证方式

- 模拟 QQ 首选播放失败、网易云成功，验证自动回退、成功历史加权、Debug 评分日志以及“下一首”继续控制网易云；
- 模拟三个已安装 Provider 全部失败，验证逐一尝试后才显示统一失败提示；
- 验证无法读取媒体信息时返回未验证启动而不拦截播放，损坏的 Provider 历史能够安全校验；
- 完整 Python 测试 161 项通过，源码编译检查通过；本机探测可输出网易云/QQ 的安装、运行、窗口、控制与评分状态。

## v0.19.1 - 2026-08-12

### 更新类型

网易云 UI 自动化可诊断性、随机播放陈楚生与桌宠高频交互修复。

### 修改内容

- 网易云真实客户端控制区分 `UI_AUTOMATION_UNAVAILABLE` 与 `RESULT_NOT_FOUND`：只有成功读取搜索结果但没有歌曲候选时才报告未找到；
- Debug 日志记录网易云 PID、主窗口句柄与标题、UIAutomation Root、一级/二级控件数、搜索框、搜索后候选数以及最终点击控件；
- 音乐入口恢复为随机播放陈楚生的基础目标，并保留播放/暂停、下一首、上一首与播放器设置；媒体 Session 暂时不可读时不阻止播放器自身开始播放；
- “和六毛聊聊”使用独立普通窗口并保留会话；聊天、工作、音乐、自习室、动作、喂食恢复为一级高频入口；
- 工作计时自动驱动陪伴动作，喂食增加食物、动作和气泡的完整反馈；旧“五大分类”测试已同步更新；
- PR 分支远端文件已从 `main` 重建，并逐一核对 Git Blob，避免接口上传造成 UTF-8 损坏或大文件截断。

### 验证方式

- 完整 pytest 共 156 项通过；
- 远端 7 个核心修改文件与本地 Git Blob SHA 完全一致；
- 在非交互 Codex 桌面环境中验证网易云进程存在但 UIAutomation Root 无子控件时返回 `UI_AUTOMATION_UNAVAILABLE`，不再误报搜索无结果；
- 真实播放的五次端到端验收仍以能够读取网易云窗口 UI 树的交互式 Windows 桌面为准。

## v0.19.0 - 2026-08-12

### 更新类型

指定歌曲精确点播、独立 Provider Adapter、媒体会话真实性校验与可诊断失败状态。

### 修改内容

- 将点歌执行流重构为 `search → exact match → play → verify`；搜索、打开客户端或触发播放控件都不再作为成功依据，只有系统当前媒体信息同时匹配歌名和歌手时才返回成功；
- QQ 音乐、网易云音乐、酷狗音乐、Apple Music、Spotify 分别使用独立 Adapter；Windows 通过 UI Automation 读取歌曲标签页、精确歌曲行和该行播放按钮，macOS Apple Music 使用 Apple Events，其他客户端在已授权 Accessibility 下读取精确结果；
- 删除旧的固定坐标、方向键、回车和“搜索后播放第一条”逻辑；拒绝歌手主页、专辑、MV、歌单及歌手不匹配结果，指定歌曲点播禁止使用全局 Play 媒体键续播旧队列；
- 播放后通过 Windows GSMTC 或 macOS Apple Events 读取当前歌名/歌手，不匹配时最多重试一次精确播放；仍不匹配或无媒体信息时明确失败；
- 新增 `SEARCH_FAILED`、`RESULT_NOT_FOUND`、`PLAY_ACTION_FAILED`、`MEDIA_SESSION_TIMEOUT`、`TRACK_VERIFY_FAILED`，界面显示对应易懂文案；debug 日志记录请求歌曲、请求歌手、最终候选、当前歌曲/歌手和失败阶段；
- 点歌闭环在既有音乐工作线程中异步执行，不阻塞桌宠动画、拖动、计时或基础播放器控制。

### 验证方式

- 模拟歌手主页、专辑、错误歌手、正确歌曲和随机歌手结果，确认只会选择歌曲类型且歌手精确匹配的候选；
- 验证五个失败状态、媒体超时、错误歌曲重试一次、调试日志字段以及点歌路径绝不调用全局媒体键；
- 运行完整 pytest、Python 编译、源码冒烟、Windows 构建与公开包隐私扫描。

## v0.18.1 - 2026-08-12

### 更新类型

macOS Codex CLI 绝对路径识别、30 轮连续聊天记忆与桌宠入口/自习室界面重构。

### 修改内容

- macOS 图形应用使用 `/bin/zsh -lc 'command -v codex'` 获取真实绝对路径，随后以同一路径执行 `--version` 和 `login status`；补齐 npm/nvm 安装场景所需的 Node 路径，并缓存成功结果供后续调用复用；
- ChatGPT.app 与 Codex CLI 独立检测。只发现 ChatGPT 不再视为 Agent 可调用；CLI 缺失、检测中、掉线或异常均直接使用离线对话，绝不自动打开设置；
- 新增仅驻留内存的 `ConversationMemory`：30 轮以内保留完整原始对话；超过后滚动压缩更早内容，只保留重要人物、话题、近期状态、约定与偏好，同时继续保留最新 30 轮；
- 陪伴 Agent 的上下文严格限制为角色设定、长期摘要、最近 30 轮和少量宠物当前状态，不读取项目代码、工作区文件或开发上下文；
- 右键菜单收拢为“聊天与陪伴、动作与外观、音乐与娱乐、专注与自习、系统与显示”五组；解锁的造型入口恢复可点击，切换、动作与等待操作均给出气泡、状态或加载反馈；
- “六毛搭子自习室”重构为首页、聊天、专注、我的四标签功能页，登录/注册改为页内 Tab；增加状态横幅、卡片层级、未登录预览、空状态与操作等待反馈，不再以大面积空白登录框作为整个页面。

### 验证方式

- 模拟 macOS nvm/npm 安装的 Codex，验证 zsh 定位、绝对路径版本/登录检查、PATH 补齐和缓存复用；
- 验证 30 轮不摘要，第 31 轮起仅滚动摘要最旧完整轮次，并检查 Agent 提示不包含项目上下文；
- 验证五个一级菜单及其子入口、自习室四标签、登录/注册 Tab、空状态与未登录反馈；
- 完整运行 Python 测试、源码编译、Windows 打包冒烟与公开包隐私扫描。

## v0.17.0 - 2026-08-12

### 更新类型

Windows/macOS 本机播放器原生控制、真实能力状态与指定歌曲搜索解耦。

### 修改内容

- 新增 `MusicProviderManager` 与异步 `MusicController`，播放控制不再通过启动 EXE 或打开客户端来伪装完成；
- Windows 优先通过 `Windows.Media.Control` 的全局媒体 Session 识别 QQ 音乐、网易云、酷狗、Apple Music 与 Spotify，并执行播放、暂停、切换、上一首、下一首和当前歌曲读取；
- 只有播放器正在运行但没有暴露可控制 Session 时才发送系统媒体键，并显示“仅支持基础控制”；只找到安装文件时仅显示“已检测应用”；
- macOS 的 Apple Music 与 Spotify 使用各自的 AppleScript/Apple Events adapter；缺少 Automation 权限时给出系统设置路径，不绕过权限；其他播放器运行时使用媒体键 adapter；
- 基本播放控制与指定歌曲搜索完全分开。QQ 音乐和网易云没有稳定公开桌面 API，因此不伪造 API 接入；Spotify 高级 OAuth 点歌保留为可选增强方向，不影响所有用户的基本控制；
- 快捷口袋与右键菜单新增播放/暂停、上一首、下一首和正在播放，命令在线程中执行，不阻塞宠物动画、拖动与计时；
- macOS 包增加 Apple Events 用途说明，Windows 包增加 PyWinRT 媒体控制依赖。

### 验证方式

- 模拟五类 Windows 媒体 Session，验证平台识别、歌曲元数据、原生命令优先和无 Session 媒体键回退；
- 模拟 macOS Apple Music 成功控制、当前歌曲读取、Automation `-1743` 拒绝和非原生播放器基础控制；
- 回归检查指定歌曲搜索文案不得出现“已连接”或保证“正在播放”。

## v0.16.1 - 2026-08-12

### 更新类型

聊天回车误触设置窗口修复与设置入口全局门禁。

### 修改内容

- 查明聊天窗口基于 `QDialog`，非发送按钮可能被 Qt 自动选为默认按钮；输入框回车在发送消息之外可能误触“AI 设置”；
- 聊天面板的 AI 设置、重新连接、去设置和发送按钮全部关闭 `autoDefault/default`，回车只由输入框提交消息；
- 将设置窗口创建收口到 `open_settings(source)`，只有 `source == "user_action"` 才会实例化对话框，其他来源直接拒绝并写入 debug 日志；
- 聊天面板、快捷口袋、右键菜单和系统托盘四个用户入口显式传递 `user_action`；Agent checking、disconnected、error、timeout、CLI 缺失和调用失败只更新状态或离线降级；
- 新增全局静态约束，禁止旧设置别名和 Agent 管理模块引用设置窗口。

### 验证方式

- 模拟在聊天输入框连续按回车发送 10 条消息，设置请求信号保持为零；
- 在 connected、checking、disconnected、error、timeout 五种状态下分别连续发送 10 条消息，设置对话框创建次数均为零；
- 非用户来源逐项调用设置入口均被拒绝，只有显式用户来源能够创建窗口。

## v0.16.0 - 2026-08-12

### 更新类型

AI Agent 增强化、聊天无缝离线降级与一键 GitHub 发布。

### 修改内容

- 新增 `AgentManager`，启动后在线程中检测并缓存 Codex、Claude Code、DeepSeek、Kimi 和兼容 API，明确区分 `checking`、`connected`、`disconnected`、`error`；
- 聊天发送只读取缓存，不再逐条运行完整连接检测；`checking` 期间直接使用本地陪伴，不误报断开也不等待；
- 新增 `OfflineDialogueManager`，结合关键词/意图、随机回复、当前时间、今日工作时长、心情、精力、饱食和专注星生成回复；
- 新增 `ChatManager`，仅在缓存已连接时异步调用 AI；初始化失败、断开、超时和未知异常均静默降级，错误正文不污染陪伴回复；
- 复杂离线问题提示“现在是离线模式，等 AI 恢复后再帮你处理”，并显示“重新连接 AI / 去设置”按钮；两个按钮只响应用户点击；
- Agent 断开后每五分钟低频后台重连，恢复后下一条聊天自然切回 AI；任何自动路径都不会打开“连接与陪伴”窗口；
- 新增 `scripts/publish_release.ps1`，可在安装并登录 GitHub CLI 后一条命令完成测试、主分支推送、标签和 Release 创建，不打开网页。

### 验证方式

- 新增 checking 离线可聊、AI 异常后缓存 error、后续不重复调用、成功异步 AI、时间/工作/宠物状态、复杂问题按钮和不弹设置页测试；
- 继续执行全部 pytest、Python 编译、源码冒烟、Windows 构建、打包后冒烟和公开包隐私扫描。

## v0.15.0 - 2026-08-12

### 更新类型

QQ 宠物式窗口共存、置顶/桌面模式切换与 macOS Codex 识别修复。

### 修改内容

- 桌宠继续使用无边框、透明、Tool 与不接收焦点标志；Windows 额外应用 `WS_EX_TOOLWINDOW`、`WS_EX_NOACTIVATE` 和不激活的原生 TOPMOST，避免点击微信、Word 等程序后被普通窗口遮挡；
- macOS 使用浮动窗口层级与跨 Space/全屏辅助行为维持可见，但不调用 `raise_()`、不激活 Lili，也不忽略宠物本体的鼠标事件；
- 透明留白继续按当前人物合成图生成窗口轮廓遮罩，人物外区域不拦截下方应用，宠物本体的点击、拖动、摸头和动画保持不变；
- 新增“始终置顶（关闭即桌面模式）”开关，可从宠物右键菜单、系统托盘或设置页切换，状态写入用户设置并跨重启保留；
- macOS 图形应用只检测 `/Applications/ChatGPT.app` 与 `~/Applications/ChatGPT.app`，用户主动打开时执行 `open -a ChatGPT`；
- Codex CLI 改为独立检测：macOS 使用 `/bin/zsh -lc 'command -v codex'` 获取绝对路径，再运行 `<absolute-path> --version`；不再扫描 `Codex.app` 内部文件；
- ChatGPT GUI 与 Codex CLI 分开显示状态；只有 ChatGPT 时显示“已检测到 ChatGPT（包含 Codex），但未检测到 Codex CLI。”，两者存在时显示“Codex 已连接”。

### 验证方式

- 新增置顶/桌面模式持久化、切换后位置/动画/遮罩/无焦点标志、macOS zsh CLI 路径、`--version` 验证、ChatGPT 单独安装状态和 `open -a ChatGPT` 回归测试；
- 全部 pytest、Python 编译检查与源码启动冒烟测试通过。

## v0.14.0 - 2026-08-12

### 更新类型

桌面共存、音乐客户端、AI 连接、设置可用性与正式安装体验修复。

### 修改内容

- 主桌宠、文字气泡和照片气泡统一使用不接收焦点、显示时不激活的窗口标记；Windows 继续以 `SWP_NOACTIVATE` 置顶，macOS 不再周期调用 `raise_()`；
- 连接设置加入滚动区，检测 Codex、Claude Code 或 API 时改用后台线程，避免小屏选项不可达和窗口假死；聊天只在用户明确点击“AI 设置”时才进入设置页；
- 音乐入口由 QQ、网易云、酷狗扩为 Apple Music 与 Spotify，增加客户端检测、应用内搜索深链和受系统辅助功能许可约束的首条播放尝试；
- macOS 图形应用环境补齐 Homebrew、原生安装器、npm、pnpm、Bun、Volta 与登录 shell CLI 路径；Claude Code 按官方 `claude auth status` JSON 返回检测，Codex 按 `codex login status` 检测；
- 缩短在线对话上下文和回答上限，Codex 使用低推理强度的临时只读会话，减少简单陪伴对话的等待；
- Windows 发布新增当前用户安装程序、开始菜单和卸载入口；macOS DMG 增加 Applications 拖放入口并显示 Dock 图标，退出后可正常重新启动。

### 验证方式

- 新增桌宠与气泡不接收焦点、连接设置滚动、五种音乐服务、CLI 图形环境和跨平台安装产物回归测试；
- 运行全部 pytest、Python 编译检查和离屏应用冒烟启动；Windows 安装器与 macOS DMG 由对应 GitHub Actions runner 构建。

## v0.13.0 - 2026-08-11

### 更新类型

小时娃衣、高分屏显示、心情能量、三平台音乐与双六毛自习室升级。

### 修改内容

- 将仓库所有者本地“娃衣”文件夹的 12 张 1254×1254 原图去除相连灰底，按 1–12 小时映射为透明高清娃衣；源文件夹不进入仓库；
- 每累计专注一小时自动换上最新娃衣，最终奖励为“荒野国王”；修复 200% 缩放时完整动作被当作双倍逻辑尺寸、只显示左上部分的问题；
- 新增可见的心情、默契、精力、饱食和专注星，吃饭、咖啡与茶会补充状态，未工作不产生负面惩罚；
- 音乐设置增加酷狗程序路径，并改进 QQ、网易云、酷狗完整客户端的窗口恢复、搜索与首条播放尝试；
- 搭子卡片突出在线、工作状态与今日时长；串门窗口显示两只高清六毛、双方今日时长和共同计时，动作只在本地轮换；
- Supabase 只新增已接受串门的统一开始时间字段，继续排除任务、聊天、窗口标题、动作帧和私人素材。

### 验证方式

- 自动检查 12 套娃衣均为 1024×1024 RGBA、具有透明像素且四边不裁切；
- 新增 200% DPR 完整动作回归测试、心情奖励测试、酷狗官方入口测试与最小社交字段测试；
- 渲染娃衣联系表、自习室和双六毛串门窗口进行人工目检，并运行完整测试与 Windows 打包。

## v0.12.0 - 2026-08-11

### 更新类型

独立动作素材替换、十种工作状态与完整动作平滑过渡。

### 修改内容

- 以仓库所有者本地“六毛”文件夹中的最终 46 张独立 PNG 替换旧动作，其中看电影、海边日光浴和睡觉使用用户最后重新修正的三张；
- 从画布边缘识别并移除生成图灰色背景，保留白色贴纸边、阴影、人物和完整道具，统一输出 1024×1024 RGBA；
- 依据旧动作不透明边界对齐中心和底部，四边至少保留 34 像素安全区，避免人物忽大忽小及桌子、乐器、背景被裁切；
- 新增花环舞步、开心开工、埋头读写、工作流转、趴桌小睡、摸鱼走神、深度专注、完成挥手、夜间加班和忙到转圈十种动作；
- 专注、休息和完成轮换使用新增工作动作；办公、编程和阅读应用类别分别显示相应的工作图；
- 完整动作切换加入 8 步、约 280 毫秒交叉淡化；不改变窗口尺寸，也不介入原有八帧走路节奏；
- 原始 1254×1254 本机图片不写入仓库，v0.11 动作保留在 Git 历史和本机构建备份中，可随时恢复。

### 验证方式

- 渲染 46 张动作联系表，目检六根红毛、蓝色发尾、小嘴、肢体、透明边缘、复杂道具和三张修正版；
- 自动检查每张素材为 1024×1024 RGBA、具有透明像素且四边不裁切；
- 窗口测试验证交叉淡化会结束、动作不改变窗口大小，完整测试与源码启动检查通过。

## v0.11.0 - 2026-08-11

### 更新类型

可选搭子自习室、私人房间、轻量在线状态与双六毛本地串门。

### 修改内容

- 新增邮箱注册/登录，密码不保存，刷新令牌只进入 Windows 凭据管理器或 macOS 钥匙串；
- 使用 8 位搭子码发送申请，接受后可查看对方在线、工作/休息状态和可选的今日累计；
- 新增私人自习室名称与房间码，可查看共同房间成员的轻量状态；
- 新增“派六毛去串门”，对方接受后双方客户端各自在本地显示两只六毛共同工作，无需同步动画坐标；
- 每 30 秒仅同步昵称、外观编号、状态、计时秒数、房间和串门事件；断网不影响本地功能；
- 新增隐身、隐藏准确时长和拒绝串门开关；不提供陌生人聊天、公开定位或强制排行榜；
- Supabase 后端启用六张表的 RLS、10 条读取/写入策略、登录用户 RPC 白名单和匿名执行撤销。

### 验证方式

- 本地测试覆盖最小同步字段、令牌边界、RLS 配置、菜单入口和原有功能；
- Supabase Advisor 检查匿名函数权限、外键索引与公开表 RLS；
- 执行源码冒烟、Windows 打包、打包后冒烟和公开包隐私扫描。

## v0.10.0 - 2026-08-10

### 更新类型

修正版 36 动作、0–8 小时每日成长、可触摸陪伴、主动事件与本机工作日报。

### 修改内容

- 依据用户最终确认的动作总图重新整理 36 张 1024×1024 透明素材，统一保留六根红毛、蓝色发尾、可见小嘴、蓝色锯齿脸、黄色连体衣与粉色爱心；
- 今日专注改为 0–8 小时成长线，每小时解锁状态、动作或彩蛋，8 小时以“荒野国王”演出和纪念卡收尾；
- 计时期间轮换办公、夜读、思考和保温杯动作；休息与完成分别使用喝茶、睡觉、音乐、比心、送花和合影动作；
- 新增头部点击歪头、来回摸头开心、点肚子护住、连续五戳小生气、拖拽、长按睡觉和悬停好奇；
- 六毛会结合早中晚、连续工作、长时间无操作主动出现，并加入 UFO、海鸥、踢球、鲸鱼和低概率荒野国王等随机事件；
- 新增只在本机保存的每日陪伴统计、六毛工作日报和相册；不记录任务名称、聊天内容、窗口标题或输入内容；
- 心情只提供“悠闲的一天、慢慢进入状态、默契 +1、精神满满”等正反馈，不设置饥饿、生病或掉好感惩罚；
- 原有十四套娃衣调整为长期收藏，与当天成长线分开。

### 验证方式

- 检查 36 张动作透明背景、统一画布、六根毛、蓝色发尾、嘴巴、裁切和人物一致性；
- 测试 0–8 小时阶段、正向心情、统计跨日重置、工作日报生成和触摸行为；
- 执行完整自动化测试、源码冒烟、Windows 打包、打包后冒烟和公开包隐私扫描。

## v0.9.0 - 2026-08-10

### 更新类型

本机音乐路径、独立歌词气泡、巴布达原声交互、全幅音乐动作与十四套每小时成就。

### 修改内容

- 设置页新增 QQ 音乐、网易云音乐本地程序路径，主动播放时优先使用用户选择的客户端；
- 歌词/歌名意象改用独立计时器，默认约八分钟一次，并支持 2–120 分钟连续设置；
- 支持读取用户有权使用的本地逐行 TXT，公开仓库和安装包不内置受版权保护的完整歌词；
- 双击右键六毛会播放“巴布达”，可轮换同目录多段音频并略微变化语速；无本地音频时回退到系统语音；
- 吉他、打鼓和耳机改为用户确认的全幅透明动作素材，保持六根毛与统一画布；
- 新增梦想清扫员、飞行梦想家、甜梦抱抱和红毯小大人四套娃衣，累计十四小时解锁最终“荒野国王”；
- 私人原视频、提取音频、本机路径和歌词文件继续只保存在本地，不上传 GitHub。

### 验证方式

- 测试本地路径优先级、歌词编码与限制、音频变体发现、十四套解锁和公开构建隐私边界；
- 渲染三套音乐动作及十四套娃衣联系表，检查透明背景、六根毛、裁切和尺寸一致性；
- 执行完整测试、源码冒烟、Windows 打包、打包后冒烟和隐私扫描。

## v0.8.0 - 2026-08-10

### 更新类型

快捷口袋收起逻辑、音乐客户端直连、AI 连接检测、每小时成就与音乐动作视觉修正。

### 修改内容

- 快捷口袋只由双击六毛切换显示；点击任一功能后立即收起，闲置八秒或移出三秒后自动隐藏；
- AI 设置新增“检测是否连接”：Codex 使用官方 `codex login status` 退出码，Claude Code 读取认证状态，API 通道只读请求 `/models`；
- Windows 优先直接启动已安装的 QQ 音乐或网易云音乐，短暂聚焦客户端、自动搜索随机歌名并尝试播放第一条；
- macOS 将正版搜索地址交给指定音乐客户端；未安装客户端或启动失败时才回退到官方网页；
- 手动音乐动作两分钟内不再被应用感知覆盖，随机音乐动作保持三分钟；
- 放大耳机、吉他和鼓，增强颜色对比并调整位置，避免动作存在但看不清；
- 成就门槛由八小时改为每累计一小时一套，共十套；首套改为折纸帽，最后一套固定为金皇冠与披风“荒野国王”；
- 版本更新为 `v0.8.0`，公开包继续排除私人素材、聊天记录、令牌和本机计时文件。

### 验证方式

- 测试覆盖本机 Agent 登录检测、API 只读检测端点、客户端缺失网页回退、快捷口袋切换与每小时成就；
- 批量渲染音乐动作及十套配饰，重点检查耳机、吉他、鼓和荒野国王的可见度；
- 执行完整测试、源码冒烟、Windows 打包、打包后运行和 GitHub 三平台发布构建。

## v0.7.0 - 2026-08-10

### 更新类型

六毛情境陪伴、工作控制、Claude Code 联动、音乐动作、健康提醒与八小时成就。

### 修改内容

- 文件与应用继续叫 `Lili`，角色称呼恢复为“六毛”，人格设定为带孩子气的“小小大人”；
- 新增 Claude Code 本地登录通道，采用一次性、无工具、无会话持久化调用；Codex 仍使用临时只读会话；
- 计时期间在六毛身前绘制电脑，点击电脑或摸头出现独立暂停/结束气泡，删除菜单里的重复控制；
- 双击六毛打开快捷口袋，减少对 Windows 托盘与 macOS 菜单栏的依赖；
- 默认高度从 180 缩小到 160，新增 100–360 像素连续滑块，所有动作共享统一画布尺寸；
- 前台应用只被归类为音乐、办公、编程、阅读或其他，据此切换耳机、电脑、写字和看书动作，不记录标题；
- 新增吉他、鼓、耳机动作，以及网易云音乐/QQ 音乐的正版歌曲搜索入口；
- 新增可关闭的“巴布达”语音、喝水提醒、站立休息、整点报时和歌名意象原创短句；
- 每累计工作八小时解锁一套娃衣或配饰，共十套，并保留终身累计秒数；
- 对话框改为约六成可见的浅灰蓝半透明背景、细灰边框，移除红色 PPT 式视觉，macOS 优先苹方简体；
- Windows 使用不抢焦点的原生置顶补强，macOS 使用 Qt Tool/WindowStaysOnTop 与周期恢复；
- 未内置歌词、音频或私人素材；只保存歌名及原创意象句，实际播放由用户在正版平台确认。

### 验证方式

- 完整测试覆盖 Claude stdin 安全调用、应用分类、正版音乐域名、双提醒、八小时解锁及原有动画；
- 本地批量渲染电脑、耳机、吉他、鼓和十套配饰，检查六根毛、蓝色脸、透明背景与统一画布；
- Windows 源码冒烟测试与 PyInstaller 构建完成后扫描公开包，确认不包含 `user_assets`、令牌或聊天记录。

## v0.6.0 - 2026-08-10

### 更新类型

Lili 品牌升级、可选 AI 联动、聊天界面、饮品、多段动作、自动牢骚与报时。

### 修改内容

- 应用显示名和公开构建产物改为 `Lili`，版本更新为 `v0.6.0`；
- 默认保持纯离线，新增 Codex、DeepSeek、Kimi 和其他 OpenAI 兼容 API 通道；
- Codex 通过本机 `codex exec` 的临时、只读会话联动，找不到、未登录或连接失败时自动回退到离线回答；
- DeepSeek 默认使用 `deepseek-v4-flash`，Kimi 默认使用 `kimi-k3`，API 地址和模型允许用户修改；
- API 令牌通过系统安全凭据库保存，非敏感连接设置保存在 Lili 本地设置目录；
- 单行输入框替换为圆角暖色聊天面板，显示提供方、隐私提示、对话区域和 AI 设置入口；
- 新增咖啡、热茶，以及伸展、思考、安静陪伴、击掌等多段组合动作；
- 新增随机本地牢骚和整点报时，两项均可开关，报时默认关闭；
- 旧“六毛工作搭子”的窗口尺寸与工作计时可在首次启动时兼容读取或迁移；
- 公共发布包继续排除 `user_assets`、私人照片、API 令牌、聊天记录和本机设置。

### 验证方式

- AI 请求、Codex JSONL 解析、HTTPS 地址校验和无明文令牌配置均使用模拟测试；
- 窗口测试覆盖新聊天面板、饮品、多段动作、牢骚与报时开关；
- 发布前运行完整测试、源码冒烟测试、Windows 构建和公开包隐私扫描。

## v0.7 - 2026-08-10

### 更新类型

工作计时、长期工作劝慰、陪伴动作与鼓励性对话增强。

### 修改内容

- 新增只保存在本机的今日工作计时，支持开始、暂停、完成和查看累计时长；
- 计时跨应用重启保留当天累计，日期变化自动归零，自然退出时自动暂停；
- 连续工作约 25 分钟显示投入鼓励，约 50 分钟提醒喝水活动，90 分钟后更明确地劝休息；
- 新增陪我专注、为我加油、给我抱抱、庆祝完成、安慰我和提醒休息六类动作；
- 扩充工作压力、自我怀疑、拖延、犯错、孤独、委屈和爱意等离线话题回应；
- 右键菜单和系统托盘都增加工作计时与陪伴动作入口；
- 公开版本号更新为 `v0.5.0`，继续沿用“六毛工作搭子 / SixHairWorkmate”名称。

### 隐私边界

- 工作记录仅包含本地日期与累计秒数；
- 不保存任务名称、对话文本或提醒历史；
- 不访问网络，不把本地工作数据打进公开安装包。

### 验证方式

- 纯逻辑测试覆盖计时开始、暂停、重复开始、跨重启累计、日期归零、格式化和三档提醒；
- 窗口测试覆盖陪伴动作、工作开始、50 分钟劝休息、完成庆祝和菜单入口；
- 发布前继续执行完整测试、三平台构建与公开包隐私检查。

## v0.6 - 2026-08-10

### 更新类型

六毛工作搭子命名、默认尺寸优化、喂食与离线对话。

### 修改内容

- 应用显示名称和公开安装包名称改为“六毛工作搭子 / SixHairWorkmate”；
- 默认显示高度从 220 调整为 180，并使用独立设置目录避免旧尺寸覆盖；
- 新增苹果、小饼干和热牛奶三种喂食选项及饱食度反馈；
- 新增桌面对话输入框、跟随人物的文字气泡和常见工作/情绪话题回复；
- 对话完全离线处理，不保存聊天历史，不访问网络；
- 托盘与右键菜单新增喂食、对话和查看状态入口；
- Windows 与 macOS 构建产物统一改用 `SixHairWorkmate` 名称。

### 验证方式

- 喂食增益、吃饱拒食、未知食物、工作对话、疲惫对话和回退回复均有纯逻辑测试；
- 窗口测试覆盖默认 180 高度、喂食气泡与本地对话气泡；
- 公开打包仍禁止包含 `user_assets`、私人照片和聊天记录。

## v0.5 - 2026-07-17

### 更新类型

角色确认、素材授权和首次发布准备。

### 修改内容

- 将保留原画风的白裙长辫标准角色登记为候选，并由用户在图形窗口中明确确认；
- 标准角色候选哈希已锁定，旧错误候选和旧走路审批不会被复用；
- 将 `assets/` 中的公开演示美术素材设为 CC BY-NC 4.0；
- 明确程序代码和文档继续采用 MIT License，`user_assets/` 不进入公开仓库；
- 补充 GitHub 发布清单并完成本机路径、常见密钥、大文件和忽略规则审查。

### 修改原因

- 完成标准角色的人机确认闭环；
- 让代码与美术素材使用不同且明确的授权，避免开源边界含糊；
- 确保首次提交不携带本地角色、原图、构建缓存或机器相关配置。

### 影响范围

- 影响素材授权说明、README、发布清单和本地角色工作流状态；
- 标准角色仍位于被 Git 忽略的 `user_assets/`，不会进入公开提交。

### 验证方式

- 用户通过图形确认窗口批准标准角色；
- 工作流显示标准角色为 `approved`，走路仍为 `not_generated`；
- `git add --dry-run .` 未包含 `user_assets`、`.venv`、`build` 或 `dist`；
- 公开文本未发现本机绝对路径或常见密钥格式。
- 首次提交已推送到公开仓库，远程 README 可读取；
- 首次 GitHub Actions `tests` 工作流结论为 `success`。

### 遗留问题

- 新标准角色的惊讶动作与八相位走路仍需在后续生成和视觉验收。

### 后续待办

- [x] 创建首次 Git 提交并推送 GitHub；
- [x] GitHub Actions 首次运行通过；
- [x] 发布 `v0.1.0` 公开构建并执行下载后验证。

### 发布后验证

- `v0.1.0` 标签已推送；
- Windows ZIP 大小为 58,149,391 字节，包含 216 个条目且不含 `user_assets`；
- GitHub 下载文件 SHA-256 为 `E8D83DCFDCA8FDD5D8F7B0DEDD4A7590A3DA349AE4D7D9268DBF7BBEE8F1CD0C`；
- 下载包解压后 `OnePicDesktopPet.exe --smoke-test-ms 1500` 退出码为 0。

## v0.4 - 2026-07-17

### 更新类型

开源流程修正、表情交互增强和审批安全加固。

### 修改内容

- 新增与人物图片解耦的矢量表情符号层，覆盖开心、害羞、惊讶、生气、困倦、疑惑、自拍、挥手和拖拽；
- 表情符号支持高 DPI、尺寸调整、轻微漂浮动画和透明窗口轮廓；
- 将固定 Q 版流程改为保留原画风、轻度 Q 版、完整 Q 版三种选择；
- 角色候选登记后保存 SHA-256，候选内容发生变化时旧确认自动失效；
- 移除 `approve-character --yes`，改为显示候选图的用户确认窗口；
- 新增错误角色撤销能力，撤销后同步废止旧走路审批；
- 增加测试专用演示素材模式，使公开测试不受本机 `user_assets/` 污染；
- 明确惊讶动作为微张嘴、单手轻放胸前的克制反应，禁止双手高举和大张嘴尖叫模板。

### 修改原因

- 让表情增强适用于开源后新增的任意角色，不需要把符号画死在素材中；
- 防止 Agent 用一个命令代替用户确认，也防止修改候选后复用旧审批；
- 尊重输入角色原有画风和辨识度，不强制把所有角色变成相同 Q 版模板。

### 影响范围

- 影响桌宠窗口绘制、标准角色登记与确认、Agent 执行说明、素材规范和测试；
- 不改变自拍必须使用登记原图、走路需单独确认和公开构建默认排除 `user_assets/` 的规则。

### 验证方式

- 完整 38 项测试通过；
- 表情状态映射和效果计时器均有自动测试；
- 已生成八种状态联系表并完成视觉验收，符号不依赖系统字体；
- 公开 EXE 重新构建成功，冒烟测试退出码为 0，构建目录不包含 `user_assets/`；
- 修改后的候选哈希无法通过审批；撤销角色后动作与走路门禁恢复为未完成。

### 遗留问题

- 新标准角色预览已经生成，但尚待用户明确确认并登记；
- 惊讶动作和全套动作必须在标准角色确认后重新生成并视觉验收。

### 后续待办

- [x] 生成一张保留原画风和身份特征的标准角色预览；
- [ ] 用户在确认窗口验收候选；
- [ ] 以确认图为唯一参考重做自然惊讶动作和八相位走路。

## v0.3 - 2026-07-17

### 更新类型

流程修正、动作优化和隐私功能完善。

### 修改内容

- 新增 `onepic_workflow.py` 和私有 `workflow.json` 状态；
- 登记原图时保留原始文件副本，并自动生成同分辨率 `selfie.png`；
- 新增首张 Q 版候选登记和显式确认门禁，未确认时禁止批量生成动作；
- 新增八帧走路 GIF 预览和显式确认门禁，未确认时不加载或打包私有宠物；
- 走路移动曲线由“落脚完全冻结后猛跳”改为 16 ms 亚像素减速—加速曲线；
- 默认水平速度从约 125 像素/秒调整为约 62.5 像素/秒；
- 公开构建默认排除全部 `user_assets/`，个人构建必须显式传入 `-IncludeUserAssets`；
- 更新 README、Agent 规则、执行说明书、素材规范和验收清单。

### 修改原因

- 防止不同人物被生成成相同模板脸；
- 防止未经确认的角色继续生成大量错误动作；
- 减少走路平移、停顿和突然加速；
- 确保自拍使用最初上传的原照片，而不是生成动画帧。

### 影响范围

- 影响私有角色生成、加载、个人构建和 Agent 执行顺序；
- 公开演示角色仍可直接运行，公开构建不含用户私有素材。

### 验证方式

- `35 passed`；
- 原图与自拍图像尺寸均为 `381×524`；
- Q 版未确认时 `prepare_user_pet.py` 返回退出码 2；
- Q 版或走路未确认时个人构建被阻止；
- 公开构建不存在 `_internal/user_assets`；
- 公开 EXE 冒烟测试退出码为 0。

### 遗留问题

- 当前测试原图尚未生成并确认新的 Q 版候选；
- 旧的偏写实全身补全图不会被自动视为已确认 Q 版。

### 后续待办

- [ ] 针对当前原图生成 2～3 个保留个人特征的 Q 版候选；
- [ ] 用户确认其中一个候选后重新生成动作；
- [ ] 展示新走路 GIF 并完成第二次确认。

## v0.2 - 2026-07-17

### 更新类型

环境验证、测试验证和构建验证。

### 修改内容

- 将 PowerShell 脚本保存为 Windows PowerShell 5.1 可识别的 UTF-8 BOM；
- 在项目内创建 `.venv` 并安装 Python 3.12 开发依赖；
- 从最终透明图集重新生成 38 个动作帧；
- 调整无私有自拍照片时的横竖图高 DPI 测试；
- 生成 `dist/OnePicDesktopPet/OnePicDesktopPet.exe`；
- 检查构建目录的私人图片和用户素材内容。

### 修改原因

- 确认环境脚本、源码、素材管线和 Windows 构建均可复现；
- 保证公开候选包不会携带私人自拍照片。

### 影响范围

- 只影响 OnePic Desktop Pet 开源候选副本；
- `.venv/`、`build/` 和 `dist/` 均被 Git 忽略。

### 验证方式

- 环境检查通过：Python 3.12.9、Git 2.51.2；
- `32 passed`；
- 源码冒烟测试通过；
- 打包 EXE 以退出码 0 完成 1.5 秒启动测试；
- 构建目录未发现 `selfie-photo-private`、`assets/personal` 或 JPG/JPEG 私人图片。

### 遗留问题

- 公开提交前仍需仓库所有者确认演示角色素材的授权范围。

### 后续待办

- [ ] 确认素材授权条款；
- [ ] 创建首次 Git 提交；
- [ ] 创建 GitHub 远程仓库和 v0.1.0 Release。

## v0.1 - 2026-07-17

### 更新类型

初始化、结构调整和隐私隔离。

### 修改内容

- 新建独立的 OnePic Desktop Pet 干净开源候选副本；
- 将 Python 包整理到 `src/onepic_desktop_pet/`；
- 迁移最终演示动作帧和必要透明图集，不迁移原始照片、错误草稿、缓存与构建产物；
- 将应用、配置目录和打包名称统一为 OnePic Desktop Pet；
- 自拍照片改从被 Git 忽略的 `user_assets/` 读取；
- 新增环境检查、安装、运行、测试和打包脚本；
- 新增 README、隐私说明、素材规范和 Agent 执行说明书。

### 修改原因

- 建立可公开、可复现且不会误传私人照片的 GitHub 仓库基础；
- 让 Agent 从环境检查开始执行，而不是只提供一段提示词。

### 影响范围

- 只影响新建的开源候选副本；
- 不修改原有的私人桌宠开发版。

### 验证方式

- 本记录创建时尚未验证；验证结果见 v0.2。

### 遗留问题

- 正式公开前需要确定演示角色图片的最终授权条款；
- 尚未创建 GitHub 远程仓库和 Release。

### 后续待办

- [ ] 完成干净环境测试；
- [ ] 完成 Windows 构建并检查打包资源；
- [ ] 确认素材授权后创建 GitHub 仓库。
## v0.21.7 - 2026-08-13

- 修复两台电脑的自习室状态不同步：开始、暂停、结束工作会立即发送 presence，不再等待下一次 30 秒心跳。
- 后台心跳返回的 dashboard 会直接刷新已打开的自习室页面，避免页面继续显示旧的“休息中”。
- Supabase `lili_dashboard()` 新增 `me_presence`、`status`、`session_seconds` 与 `last_seen`，统一返回 `focus`、`rest`、`offline` 状态；自习室同时展示本人和搭子。

## v0.21.6 - 2026-08-13

- macOS Codex 检测首先使用 `/bin/zsh -lc 'command -v codex'`，并在 GUI 环境缺少用户 shell PATH 时读取 zsh profile、交互式 zsh 与 nvm/npm/pnpm 常见目录；成功后始终缓存并使用绝对 CLI 路径。
- 增加可选 OpenAI Responses API 对话后端，继续使用长期摘要与最近 30 轮上下文，API 令牌只保存到系统凭据库。
- 增加 FocusSessionManager，让桌面计时器成为自习室唯一计时源；自习室支持专注/休息/结束控制、房间成员状态、共同目标、动态流和戳一下/加油/递奶茶互动。
